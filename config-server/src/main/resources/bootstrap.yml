spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          url: file:/C:/Users/andrea.panzani/ideaProjects/microservices-demo/config-server-repository
          clone-on-start: true
    fail-fast: true