server:
  port: 8888

logging:
  level:
    ROOT: info
    org.spring.framework.cloud.config: DEBUG

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/apanzani/spring-cloud-config-server-repository
          username: apanzani
          password: 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
          default-label: main
          clone-on-start: true
    fail-fast: true
log:
  app-name: config-server